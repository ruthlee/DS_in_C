#include <stdio.h> 

int main(int argc, char **argv)   /* int before main is type of return and the things in the parenthesis are the arguments for the actual command line argument(?)*/
{
    printf("%s\n", "Hello, world!");
    return 0;
}
